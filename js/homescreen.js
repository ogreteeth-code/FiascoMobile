//Handlebars list helper. Written generically to include all items in a "list."

// Compile homescreen template for Handlebars to use.
//item = document.querySelector(".playsetList").innerHTML;
//window.alert(item);